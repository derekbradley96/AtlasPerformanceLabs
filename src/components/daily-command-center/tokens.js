import { spacing, radii } from '@/ui/tokens';

export const dccTokens = {
  maxWidthDesktop: 1280,
  sectionGapDesktop: spacing[24],
  sectionGapMobile: spacing[16],
  cardPadDesktop: spacing[22],
  cardPadMobile: spacing[16],
  radiusLg: Math.max(20, radii.card || 16),
  radiusMd: Math.max(14, radii.md || 12),
  primaryCtaHeightDesktop: 56,
  primaryCtaHeightMobile: 52,
  supportCtaHeight: 44,
};

