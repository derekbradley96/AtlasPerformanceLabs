# Legacy logo asset (`logo-image-1.png`)

**Current UI:** `AtlasLogo` in `src/components/Brand/AtlasLogo.jsx` is a **typography-only** wordmark (no image). The PNG in this folder is **not referenced** by the app.

If you reintroduce a raster logo, import it from `AtlasLogo.jsx` and update this note.

Optional check: `file src/assets/logo-image-1.png` should report PNG/RGBA if you use a transparent asset.
