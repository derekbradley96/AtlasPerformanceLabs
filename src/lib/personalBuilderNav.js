/**
 * Canonical program builder URLs for Personal accounts (single training-creation flow).
 * Today / deep links use `from=today` for a contextual banner on ProgramBuilderPage.
 */
export const PERSONAL_PROGRAM_BUILDER = '/program-builder?personal=1';

export const PERSONAL_PROGRAM_BUILDER_FROM_TODAY = '/program-builder?personal=1&from=today';
